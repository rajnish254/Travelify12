import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    root: {
        margin: 10
    },
    link: {
        textDecoration: "none"
    },
    dialog: {
        minWidth: 500,
    },
    formControl: {
        margin: 10,
        minWidth: 250,
    },
    button: {
        float: 'right',
        marginTop: 25,
        marginRight: 80
    }
}));
